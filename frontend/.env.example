# Base URL the frontend uses to reach the backend API, including the /api suffix.
# Baked in at build time by Vite - rebuild the frontend after changing this.
VITE_API_BASE_URL=http://localhost:5000/api
